# 《逾期之书》v2.2 §五 引擎地基落地小结

> 用户指令：「对的 推进地基框架吧」——按《框架地基设计-v2.2.md》§五 把引擎地基落进 `godot/`，**剧情文案一字不动**，headless 点测 PASS 不降。

## 一、本轮落地的地基（6 项）

| # | 文件 | 改动 |
|---|---|---|
| 1 | `content/night_a.json` | 邻接重写（§2.2 楼层树）：`borrowing_desk`→`service_desk`、`rain_porch`→`entry_porch`；删 `return_box`；新增空壳 `study_zone`/`utility_zone`/`lounge_stairs`(locked)/`archive_lamp`(locked)/`void_room`(void) |
| 2 | `Main.gd` | 8 处改造：home→「⌂ 回到服务台」+绑 service_desk；区域图跳过 void、灰显 locked；`_on_goto` 拦截 void；出口循环锁定灰显不计入；F3 记忆按夜分级；F2 在 `_ready` 写 `ProgressState` |
| 3 | `autoload/ProgressState.gd` + `project.godot` | 新建跨夜进度单例（LibraryState 地基占位），已注册 autoload |
| 4 | `test_harness.gd` | 重写匹配新拓扑（出口数按树边断言、void_room 永不开启、八区常驻回服务台） |
| 5 | `tools/gen_content.py` | **同步**内嵌 data 至新拓扑 + closeup；重跑校验 `EQUAL`（重跑不会覆盖本次成果） |
| 6 | `tools/spine_check.py` + `CONTENT_SCHEMA.md` | **F4 脊柱护栏**：扫描夜序<night_d 的「点破词」（含负向先行断言排除「是你自己的事」领属误伤）；契约文档加「脊柱护栏」节与自查项 |

## 二、验证结果

- **Headless 点测**：`godot --headless --path . res://test_harness.tscn`
  → **PASS=52 / FAIL=0 / ALL GREEN**（与 07-13 基线持平，未降）。
  → 尾随 `CanvasItem/ObjectDB leaked` 为 headless quit 噪声，非测试失败。
- **Spine 护栏**：`python tools/spine_check.py` → **exit=1，命中 3 处**（reveal.stage「信是你写的」/ read_front 结算标题「信，是你写的」/ trace_next 结算正文「是你自己」）。
  → 这 3 处即已锁 **P0 红线债**（夜 A 提前点破自认），护栏正确拦出，待夜 A red-line 重写清除；护栏存在正是为防止其在后续夜复现。

## 三、明确留待（未做）

- **F1** reveal 玩家自拼合交互：属夜 D 内容，超出本轮「地基」且需剧情文案，未实现。
- **F2** 真正跨夜写盘持久化：当前仅单夜占位（night_index/unlocked_zones 由 `_ready` 写入）。
- **夜 A 内容 P0 红线重写**：用户本轮只批地基，剧情文案保持原状；红线债由护栏看守。

## 四、设计纪律（已刻入记忆）

「场景区隔须符合现实物理行为逻辑」——为凑数硬造的独立大区若可近景化则必须降级为近景子场景；判定尺子对所有人一致（v2.1 的还书归档陈列区/流动书架区即因此降为近景，升 `study_zone`/`utility_zone`）。
